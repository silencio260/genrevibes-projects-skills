# Remote config template

Use the [shared Firebase template](../../skills/remote-config/remote_config_template.json)
and [remote config instructions](../../skills/remote-config/SKILL.md).

The previous app-specific export was removed from active templates because its
values and export metadata did not represent shared kit defaults. It remains in
Git history. Keep app-specific overrides in the destination app, not this folder.
